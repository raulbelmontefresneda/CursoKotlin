package dam.movil.project1.ui.main
/*
Esta clase contiene como parámetros del constructor los valores que
son suceptibles de cambiar en la vista.
el uiState seria de este tipo
 */

data class MainState(val contador: Int, val error: String?)
//data class MainState(val contador: Int, val sumando: Int, val error: String?, )

